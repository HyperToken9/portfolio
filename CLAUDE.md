# Nathan Saldanha — Portfolio

Personal portfolio site. The Next.js app lives in `site/`; all site copy is in `site/src/content/portfolio.ts`, mirroring `portfolio-content.md`. Voice rules and project background are in `portfolio-handoff.md`.

This repo is **public**, and so are its issues. Never commit or write into an issue the confidential details of the Enterprise Search project (client, platform name, real query examples).

## Agent skills

### Issue tracker

Issues are tracked in GitHub Issues on `HyperToken9/portfolio`, via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` and `docs/adr/` at the repo root. See `docs/agents/domain.md`.
